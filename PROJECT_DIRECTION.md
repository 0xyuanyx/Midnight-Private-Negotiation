# Private Negotiation 프로젝트 방향성

마지막 업데이트: 2026-09-17 (KST)

이 문서는 해커톤 출품을 위한 제품 방향과 검증 범위를 기록한다. 실제 구현·실행 방법은 코드와 [README](README.md)를 기준으로 확인한다.

## 확정된 방향

- Midnight Korea Hackathon에 개인 프로젝트로 출품한다. 참가 등록은 완료했다. 아카데미 수료는 예정이며, 주최 측 세션에서 요청받아 시작한 프로젝트의 출품 허락을 받았다. 본사 코드 리뷰는 아직 완료되지 않았다.
- 구매자와 판매자의 한도를 공개하지 않고 협상하고, 합의 시 최종 금액만 Midnight에 기록하는 DApp을 계속 고도화한다.
- 제출 시나리오는 **GPU 서버 10대의 비공개 B2B 견적 협상**으로 좁힌다. 이는 실제 고객 검증을 마친 제품이 아니라, 예약 가격 보호를 설명하기 위한 첫 적용 시나리오다.
- 발표와 README에서는 구매자의 최대 승인 한도와 판매자의 최저 수용가가 상대방·AI·Relay·공개 원장에 전달되지 않는다고 설명한다. 현재 로컬 Demo Controller는 데모 신뢰 경계 안에서 한도를 역할 런타임으로 전달하므로, 플랫폼 전체가 한도를 볼 수 없다고 주장하지 않는다.
- 거래가 성립하면 최종 합의 가격은 공개한다. 이 제출은 예약 가격과 실패한 제안의 보호를 다루며, 최종 계약가 비공개 제품을 주장하지 않는다.
- 이번 제출에서 구매자·판매자 브라우저를 실제 독립 클라이언트로 분리하지 않는다. 한 화면의 Buyer·Seller·Observer 콘솔을 발표용 데모로 유지한다.
- 본사 피드백 반영, 새 클론에서 실행되는 버전·의존성 정합성, README, 발표 가능한 라이브 데모를 우선한다.
- 다른 개발자가 클론한 환경에서 설치·컴파일·테스트·실행 흐름을 재현할 수 있어야 한다. 구현이나 검증이 끝나지 않은 기능은 README에서 완료로 표현하지 않는다.

## 현재까지 확인한 상태

- 2026-09-11 기준 새 클론의 `bootstrap`, 루트 테스트 36/36, 웹 빌드·테스트 8/8을 확인했다. 보안 감사는 루트 0건, 웹 high 2건·moderate 4건이다. 웹 잔여 항목은 Vinext와 Drizzle 개발 도구 경로에 있다.
- 실제 OpenAI 협상 평가에서 모델 요청 31회, 모델 선택 5회, 합의 2건과 결렬 1건을 확인했다. 이 평가는 당시 작업 폴더의 별도 미커밋 협상 로직을 포함한다.
- mock 협상과 로컬 Midnight Node·Indexer·proof server의 전체 흐름에서 Observer의 `OPEN → AUTHORIZED → SETTLED`를 확인했다. 공개 테스트넷·메인넷 배포는 확인하지 않았다.
- 기존 안정화 변경은 `528d7b9`에 반영됐다. 협상 전략은 `005e6da`, 제3자 정산 거절 테스트는 `cdc418b`, B2B 화면 문구는 `85cc890`에 별도로 반영했다.
- 2026-09-17 루트 테스트 44/44, 웹 테스트 8/8, 계약 집중 테스트 5/5가 통과했다. 로컬 체인 성공 시나리오에서 Observer의 `OPEN → AUTHORIZED → SETTLED`와 성립 후 최종 가격 `100,000,000 KRW` 공개를 확인했다. Buyer `90000000` / Seller `95000000`의 비중첩 시나리오는 `OPEN → CANCELLED · 공개된 금액 없음`으로 종료됐다. 상세 명령과 신뢰 경계는 README, 실행 근거는 재현성 기록에 남겼다.
- `4d95515`까지 원격 `main`에 푸시했다.
- 2026-09-17에 공개 원격의 최종 커밋 `4d95515`를 새로 클론해 `bootstrap`, `typecheck`, 루트 테스트 44/44, 웹 `npm ci`, 웹 테스트 8/8이 통과하는 것을 확인했다. 이 검증은 Docker 로컬 체인 재기동을 포함하지 않는다. 공개 테스트넷 배포와 호스팅된 라이브 URL은 여전히 미실행이다.
- OpenAI provider 배선은 실제 API 1회 호출로 확인했다. `gpt-5.6-sol`이 공개 입력만으로 유효한 후보를 반환한다. AI 후보 생성 실패는 설계상 조용히 로컬 fallback으로 전환되므로 화면에서 실제 모델 사용 여부를 구분할 수 없다.

## 다음 작업 방향

1. 성공, 가격 미중첩 결렬, 제3자 정산 거절의 세 경로를 발표에서 재현 가능한 상태로 유지한다.
2. 제출 서사에서 Business Value를 보강한다. 계약은 이미 협상 이전에 한도를 commitment로 고정하고 정산 시 재검증하므로, "상대방에게 숨긴다"에 더해 "사전에 구속된 한도를 공개하지 않고 준수 여부를 증명한다"는 성질을 설명한다. 이때도 로컬 Demo Controller 신뢰 경계를 함께 명시하며, 실제 고객·감사 도입 사실은 주장하지 않는다.
3. 한국 시장의 구체적 페인포인트와 사업모델은 후속 탐색으로 두며, 고객 검증 전에는 확정된 BM으로 표현하지 않는다.
4. 이번 제출에서 독립 Buyer·Seller 클라이언트 재구축은 하지 않는다. 마감(2026-09-28)까지의 잔여 기간과 현재 데모의 동작 상태를 우선한다.

## Drivacy와의 구분

Claude Code의 Midnight Expert로 **초기 개발 틀을 만들 계획은 [Drivacy](../drivacy/PROJECT_DIRECTION.md)에 기록**되어 있다. 이 저장소는 이미 구현된 DApp이므로 동일한 초기 스캐폴드 계획을 자동 적용하지 않는다.

## Claude Code 제출 작업 문서

이번 제출 방향의 설계·구현 순서는 다음 문서를 기준으로 한다.

- [B2B 비공개 견적 협상 설계](docs/superpowers/specs/2026-09-15-b2b-private-quotation-submission-design.md)
- [B2B 비공개 견적 협상 구현 계획](docs/superpowers/plans/2026-09-15-b2b-private-quotation-submission.md)
- [Claude Code 작업 인계](docs/claude-code/2026-09-15-submission-handoff.md)

## 대화에서 방향이 바뀌면

새로 합의된 내용을 이 문서의 해당 섹션에 반영하고 날짜를 갱신한다. 확정·제안·미결정과 검증 상태를 구분한다. 실행법이나 실제 구현 상태가 달라지면 README도 함께 갱신한다. 개인적인 대화와 비밀값은 기록하지 않는다.
